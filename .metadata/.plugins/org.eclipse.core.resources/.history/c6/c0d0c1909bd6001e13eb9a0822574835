<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
<style>
  .btn-wrap {
    margin-top: 10px;
  }
</style>
</head>
<body>

  <form id="frm"
        method="POST"
        enctype="multipart/form-data">
  
    <div class="inp-wrap">
      <label for="title">제목</label>
      <input type="text" name="title" id="title">
    </div>
    <div class="inp-wrap">
      <label for="attach-file">첨부</label>
      <input type="file" name="attach-file" id="attach-file">
    </div>
    <div class="btn-wrap">
      <button type="submit">전송</button>
      <button type="reset">다시입력</button>
    </div>
  
  </form>
  
  <hr>
  
  <div id="preview">미리보기영역</div>

  <script>
    function isImage(file) {
    	const contentType = file.type;
    	return contentType.startsWith('image');
    }
    
    function preview(file) {
    	const fileReader = new FileReader();
    	fileReader.readAsDataURL(file);
    	fileReader.addEventListenr('load', (evt)=>{
    		// console.log(evt.target);
    		document.getElementById('preview').innerHTML = `<h3>미리보기영역</h3><div><img src=
    	})
    }
    
    function checkFile(inp) {
    	if(isOversize(inp.files[0])){
    		alert('첨부 파일의 최대 크기는 5MB입니다.');
    		inp.value = '';
    		return;
    	}
    	if(!isImage(inp.files[0])){
    		alert('이미지 파일만 첨부할 수 있습니다.');
    		inp.value = '';
    		return;
    	}
    	preview(inp.files[0]);
    }
  </script>
</body>
</html>