<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdu.prj.dao.board_t">

<insert id = "insertBoard"
        parameterType="com.gdu.prj.dto.BoardDto">
  INSERT INTO BOARD_T (
      BOARD_NO
    , TITLE
    , CONTENTS
    , MODIFIED_AT
    , CREATED_AT
  ) VALUSE (
      BOARD_SEQ.NEXTVAL
    , #{title}
    , #{contents}
    , CURRENT_DATE
    , CURRENT_DATE 
  )
  
</insert>

<delete id = ""></delete>

<select id = "selectBoardList"
        parameterType="Map">
  SELECT BAROD_NO, TITLE, CONTENTS, MODIFIED_AT, CREATED,AT
    FROM (SELECT ROW_NUMBER() OVER(ORDER BY BOARD_NO #{sort}) AS RN, BOARD_NO, TITLE, CONTENTS, MODIFIED_AT, CREATED_AT
          FROM BOARD_T)
   WHERE RN BETWEEN #{begin} AND #{end}
</select>

</mapper>